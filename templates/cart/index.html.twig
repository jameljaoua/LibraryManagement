{% extends 'base.html.twig' %}

{% block title %}Mon Panier{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">🛒 Mon Panier</h1>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% if cart and cart.items|length > 0 %}
        <div class="table-responsive">
            <table class="table table-bordered">
                <thead class="table-light">
                    <tr>
                        <th>Livre</th>
                        <th>Prix unitaire</th>
                        <th>Quantité</th>
                        <th>Sous-total</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in cart.items %}
                        <tr>
                            <td>
                                <strong>{{ item.livre.titre }}</strong><br>
                                <small class="text-muted">
                                    {% for auteur in item.livre.auteurs %}
                                        {{ auteur }}{% if not loop.last %}, {% endif %}
                                    {% endfor %}
                                </small>
                            </td>
                            <td>{{ item.livre.prix|number_format(2, '.', ' ') }} €</td>
                            <td>{{ item.quantity }}</td>
                            <td><strong>{{ item.subtotal|number_format(2, '.', ' ') }} €</strong></td>
                            <td>
                                <a href="{{ path('app_cart_remove', {id: item.id}) }}" 
                                   class="btn btn-danger btn-sm"
                                   onclick="return confirm('Êtes-vous sûr de vouloir retirer cet article ?')">
                                    <i class="fas fa-trash"></i> Retirer
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <td colspan="3" class="text-end"><strong>Total:</strong></td>
                        <td colspan="2"><h4 class="text-primary mb-0">{{ cart.total|number_format(2, '.', ' ') }} €</h4></td>
                    </tr>
                </tfoot>
            </table>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_home') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Continuer mes achats
            </a>
            <a href="{{ path('app_order_checkout') }}" class="btn btn-success btn-lg">
                <i class="fas fa-credit-card"></i> Passer la commande
            </a>
        </div>
    {% else %}
        <div class="alert alert-info text-center">
            <h4>Votre panier est vide</h4>
            <p>Ajoutez des livres à votre panier pour continuer vos achats.</p>
            <a href="{{ path('app_home') }}" class="btn btn-primary mt-3">
                <i class="fas fa-book"></i> Voir les livres disponibles
            </a>
        </div>
    {% endif %}
</div>
{% endblock %}